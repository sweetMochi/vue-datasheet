# 04 · 專案骨架建置與驗證

`2026-09-18` · 人工介入：指定範圍（只建骨架、`App.vue` 不寫內容）

## 產出

```
frontend/
├── package.json / vite.config.ts / tsconfig.json
├── eslint.config.js / .prettierrc.json / .gitignore
├── index.html
└── src/
    ├── main.ts / App.vue / style.css / env.d.ts
    └── setup.smoke.test.ts
```

`src/App.vue` 只有一層 `<main>` 外框與流程註解，無任何實作

## 安裝過程踩到的坑

| 問題 | 症狀 | 處理 |
|---|---|---|
| `@eslint/js` 版本對不上 | ESLint 本體是 10.10.0，但 `@eslint/js` 最新只到 10.0.1，照 `^10.10.0` 寫會 `ETARGET` | 改成 `^10.0.1` |
| `baseUrl` 在 TS 6 被棄用 | `TS5101: Option 'baseUrl' is deprecated and will stop functioning in TypeScript 7.0` | 移除，`moduleResolution: "bundler"` 下 `paths` 不需要它 |
| Vitest 5 的 provider 不是字串 | 寫 `provider: 'playwright'` 會啟動失敗：`Browser Mode was enabled, but provider was not specified` | 從獨立套件 `@vitest/browser-playwright` 匯入函式 |
| `defineConfig` 來源錯誤 | 從 `vite` 匯入會報 `TS2769: 'test' does not exist in type 'UserConfigExport'` | 改從 `vitest/config` 匯入 |
| browser mode 首跑重載測試 | `Vite unexpectedly reloaded a test. This may cause tests to fail, lead to flaky behaviour` | 加 `optimizeDeps.include: ['vue', 'vitest-browser-vue']` |
| locator 匯入路徑 | 不是 `@vitest/browser/context` | 是 `vitest/browser` |
| browser mode 無自動 globals | 測試檔找不到 `it` | 明確 `import { it, expect } from 'vitest'` |
| tsconfig 缺型別 | `import './style.css'` 報 `TS2882` | `types` 加入 `["vite/client", "node"]` |

## 驗證結果

| 指令 | 結果 |
|---|---|
| `npm run typecheck` | ✅ exit 0 |
| `npm run lint` | ✅ exit 0 |
| `npm run build` | ✅ 60.45 kB，gzip 23.86 kB |
| `npm run test` | ✅ 2 passed（真 Chromium） |
| `npm run dev` | ✅ HTTP 200，Network 位址有對外開 |

## 過程中的錯誤

回報套件安裝成功時讀的是 `tail` 的 exit code 而非 npm 的：

```bash
npm i -D ... 2>&1 | tail -10; echo "EXIT $?"    # ← $? 是 tail 的，不是 npm 的
```

導致第一次 devDependencies 整批因 `ETARGET` 失敗卻回報成功，之後改用 `${PIPESTATUS[0]}` 取真正的回傳碼

## 冒煙測試的定位

`src/setup.smoke.test.ts` 兩支測試只驗環境本身 — EventSource 是否原生、SFC 掛不掛得起來、Tailwind 有沒有生效，不涉任何商業邏輯

用意是證明工具鏈確實能動，開始寫真正的測試後會移除，**它不是題目要求的那「至少一支測試」**
