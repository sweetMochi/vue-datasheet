# 前端工程師 實作題

面試聊到的東西，用實作來看會比較準，所以想請你花點時間做一下這個。

- 繳交：GitHub repo 連結，public 或加我們進去都可以。我們會順便看一下 commit 歷史，不用刻意整理
- 時間：**做不完沒關係**，沒做完的部分在 README 交代一下就好
- 期限：收到後一週內給我們就可以，需要多一點時間的話跟我們說一聲
- **過程中有任何不清楚的地方，直接回信問我們。問問題不扣分，我們會回。**
- 不論結果如何，我們都會給你回饋

## 情境

使用者上傳一份文件（檢驗報告、產品規格表那類的），後端要花時間解析，把裡面的欄位抓出來，再讓使用者確認跟修改。

解析要跑十幾秒到數十秒，看文件內容多寡。

## 要做的

上傳 → 解析 → 審核，這一段的前端。

後端我們準備好了，在附件的 `mock後端` 資料夾，`docker compose up` 就會起來，API 說明在裡面的 README。`server.py` 請勿修改；`docker-compose.yml` 再麻煩你把自己的前端加進去。

附了一份 `檢驗報告_範例.pdf` 給你測試用。其實隨便丟什麼檔案都可以，後端不會真的去解析內容。

## 需求

以下是我們想到的：

- 解析要跑一段時間，別讓使用者對著空白畫面乾等
- 抓出來的欄位要能確認、修改
- 有些欄位系統自己也沒什麼把握，使用者要看得出來哪幾個需要特別檢查
- 欄位數量不固定。十幾個是常態，但也遇過上百個的
- 欄位分成幾個群組（基本資料／營養標示／檢驗結果／廠商資訊），同一組的要能一起看。後端是照文件裡出現的順序回傳的，同組的不會自己排在一起
- 有幾個欄位是法規必填的（品名、有效日期、廠商名稱）。文件裡沒抽到的話，使用者得自己補上，不能就這樣送出去
- 有些欄位系統抓到不只一個候選答案，使用者要挑一個，或是都不對就自己填
- 跑到一半使用者可能就不想等了
- 後端偶爾會出包，解析跑到一半掛掉的情況是有的

需求不少，所以先講清楚：**這些不必全部做完。**
做不完的部分，在 README 說一下你為什麼先做這個、後做那個。**先做哪一個本身就是判斷**，我們看的是這個。

沒寫到的地方，你可以自己判斷，也可以來問我們——**兩種我們都想看**。重點不是你選哪一種，是你怎麼決定的。

## 介面

這份清單一列要承載的東西不少：標籤、值、把握度、確認狀態、是不是必填、有沒有缺漏、其他候選答案、群組、頁碼。九樣東西平均分配的畫面，跟有人想過的畫面，看起來是不一樣的。

附了一張 `AI產生的介面_參考.png`，是我們把上面的需求原封不動丟給 AI，它給的第一版。請在 README 回答：

> 這張圖你覺得最嚴重的三個問題是什麼？為什麼？你自己的版本怎麼處理？

沒有標準答案，我們想看的是你的判斷依據。

## 技術

要用的：TypeScript、Tailwind，框架 Vue 或 React 都行，測試至少一支，`docker compose up` 之後能直接開來用。

測試我們看的是它防得住什麼——哪天有人改壞了，這支測試會不會紅。數量不重要。

有餘力再做的：無障礙、多一點測試覆蓋、跨裝置。

**如果時間不夠要取捨**：比起功能做得多完整，我們更想看 README 跟測試。

畫面不用做得漂亮，但要看得出你想過「使用者第一眼該看哪裡」。講不出理由的漂亮，跟沒想過的陽春，我們一樣沒辦法給分。

## 怎麼確認它是對的

請在 README 說明你**怎麼驗證這東西真的能動**——不只是「打開來會跑」。

哪些情況你實際試過、怎麼試的、有沒有試出問題。沒把握的部分直接說沒把握，這不扣分。

## 關於 AI

會用就用，不用避開。麻煩在 README 寫一下：

- 哪些是 AI 寫的、你改了什麼、為什麼改
- **這份 code 裡你最不確定的是哪一段？為什麼不確定？**

第二點我們特別想看。整份都很有把握的人很少，指得出哪裡沒把握反而是我們想要的。

## README 順便回答五件事

1. 這份需求哪裡沒講清楚？你是怎麼假設的？
2. **這個介面上，你決定不顯示、或降級顯示哪些資訊？為什麼？**
3. **這幾條需求裡，你覺得有哪兩條互相衝突？你怎麼取捨的？**
4. 有沒有什麼你決定不做？為什麼？
5. 如果能問我們三個問題，你想問什麼？

## 交件之後

會約半小時線上聊一下，你開著自己的 code 講講當時怎麼想的就好，不用準備投影片。

我們會挑幾段請你說明「這裡為什麼這樣寫、拿掉會怎樣」，畫面上也會問你「為什麼這個放在這、那個收起來」，所以交出去之前，確認每一行、每一塊你都知道它在幹嘛。
