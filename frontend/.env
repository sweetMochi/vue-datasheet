# 後端位址。Vite 在打包時字面替換進 bundle，不是執行期才讀的值。
#
# 發出請求的是使用者的瀏覽器，它跑在主機上、不在 docker compose 網路裡，
# 所以這裡不能填服務名（http://api:8000），前端進了 compose 也一樣填 localhost。
#
# 個人要改（換 port、從區域網路另一台裝置連）請寫在 .env.local，它已被 .gitignore 排除。
# 結尾不要加斜線，apiUrl() 直接字串相接。
VITE_API_BASE=http://localhost:8000
